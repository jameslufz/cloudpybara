services:
  cloudpybara:
    image: ghcr.io/jameslufz/cloudpybara:latest
    container_name: cloudpybara
    restart: unless-stopped
    env_file:
      - .env.local
    ports:
      - "127.0.0.1:3001:3000"
